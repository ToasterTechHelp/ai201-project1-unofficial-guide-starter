sentence-transformers==3.4.1
chromadb>=0.6.0
groq==0.15.0
python-dotenv==1.0.1

# Query interface — Milestone 5:
# Pinned to 6.9.0: gradio 6.10+ requires huggingface-hub>=1.2, which conflicts
# with sentence-transformers==3.4.1 / transformers 4.x (needs huggingface-hub<1.0).
gradio==6.9.0
# streamlit>=1.40.0

# Document ingestion — documents/ folder contains .pdf files:
pdfplumber==0.11.4
